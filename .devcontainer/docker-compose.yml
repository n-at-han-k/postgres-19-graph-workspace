version: '3'
services:
  # Update this to the name of the service you want to work with in your docker-compose.yml file
  devcontainer:
    # If you want add a non-root user to your Dockerfile, you can use the "remoteUser"
    # property in devcontainer.json to cause VS Code its sub-processes (terminals, tasks, 
    # debugging) to execute as the user. Uncomment the next line if you want the entire 
    # container to run as this user instead. Note that, on Linux, you may need to 
    # ensure the UID and GID of the container user you create matches your local user. 
    # See https://aka.ms/vscode-remote/containers/non-root for details.
    #
    # user: vscode

    # Uncomment if you want to override the service's Dockerfile to one in the .devcontainer 
    # folder. Note that the path of the Dockerfile and context is relative to the *primary* 
    # docker-compose.yml file (the first in the devcontainer.json "dockerComposeFile"
    # array). The sample below assumes your primary file is in the root of your project.
    #
    # image: ghcr.io/lucernae/devcontainer-nix:v1
    # NOTE: relative paths here resolve against the PRIMARY compose file's dir,
    # which is the repo root (../docker-compose.yaml is first in dockerComposeFile).
    # So everything is written relative to the repo root, not this file.
    build:
      context: ./.devcontainer
      dockerfile: Dockerfile
    privileged: true
    volumes:
      # Mount the repo root at the workspaceFolder path set in devcontainer.json.
      - .:/workspaces/postgres-19-graph-workspace:cached
      - ./.devcontainer/nix.conf:/etc/nix/nix.conf

      # Uncomment the next line to use Docker from inside the container. See https://aka.ms/vscode-remote/samples/docker-from-docker-compose for details.
      # - /var/run/docker.sock:/var/run/docker.sock

    # Share the postgres container's network namespace, so the DB is reachable at
    # localhost:5432 from inside the devcontainer (same as on the host).
    # (network_mode is incompatible with `hostname`.)
    network_mode: "service:postgres"

    environment:
      NIX_MULTI_USER: "true"
      NIX_REMOTE: "daemon"
      VSCODE_SERVER_CUSTOM_GLIBC_LINKER: "true"

    # Start the postgres stack together with the devcontainer (same compose project).
    depends_on:
      - postgres

    # Uncomment the next four lines if you will use a ptrace-based debugger like C++, Go, and Rust.
    # cap_add:
    #   - SYS_PTRACE
    # security_opt:
    #   - seccomp:unconfined

    # Overrides default command so things don't shut down after the process ends.
    command: sleep infinity
